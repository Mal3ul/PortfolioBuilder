# Server
PORT=10000
NODE_ENV=development

# JWT
JWT_SECRET=change_me
JWT_EXPIRES_IN=1d

# Brevo (API HTTPS)
BREVO_API_KEY=your_api_key_here
BREVO_SENDER_EMAIL=verified@yourdomain.com

# SMTP (optionnel si vous utilisez l’API Brevo)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=## Example env for backend
# Copy to .env and set your secret
JWT_SECRET=portfolio-builder-hopital-singe-2026
JWT_EXPIRES_IN=7d

# SMTP Outlook / Office365
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=nessia.bouchouit@ccicampus.fr
SMTP_PASS=Qrrw9294

# Optionnel
SMTP_SECURE=false

