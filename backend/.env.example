## Example env for backend
# Copy to .env and set your values

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/portfoliobuilder

# JWT
JWT_SECRET=change-me-dev
JWT_EXPIRES_IN=30m

# SMTP Brevo / neutralized mode
NEUTRALIZE_EMAIL=true
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=a05755001@smtp-brevo.com
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=PortfolioBuilder
SMTP_SECURE=false

NODE_ENV=development
PORT=10000